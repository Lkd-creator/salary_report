---
name: salary-report-template
description: Обезличенный шаблон skill для проекта автоматического расчёта зарплаты отдела продаж. Подходит для публикации в открытых репозиториях.
---

# salary-report (template)

Это **обезличенный шаблон** проекта автоматического расчёта зарплаты отдела продаж по выгрузкам из учётной системы (аналог 1С УПП). Все имена, названия организаций и суммы заменены на плейсхолдеры.

## Когда использовать

- Запустить сборку отчёта за месяц
- Проверить корректность результата (сверить с эталоном)
- Обновить эталон на следующий месяц
- Обновить справочники (состав менеджеров, посредники, шкалы премий)
- Добавить новый парсер листа
- Диагностировать ошибку

## Структура проекта

```
draft_salary_report/
├── README.md              # описание проекта
├── README-run.md          # инструкция по запуску
├── etalons/               # шаблоны Excel (НЕ перезаписываются парсером)
│   └── YYYY_MM_salary.xlsx
├── input/                 # входные данные из учётной системы за месяц
│   ├── YYYY_MM_АКБ.xls
│   ├── YYYY_MM_ЗНР_ОП_ставка.xls
│   ├── YYYY_MM_ЗНР_ОР_ставка.xls
│   ├── YYYY_MM_ЗНР_ОП+закуп всего.xls
│   ├── Букинги исключения месяц.xls
│   └── Итоги плана МЕСЯЦ.jpg
├── scripts/
│   └── salary_report.py   # главный скрипт
└── output/                # результат сборки
    ├── salary_YYYY_MM.xlsx
    └── build.log
```

## Главный скрипт

`scripts/salary_report.py` — Python 3.14, openpyxl, xlrd 2.0.1, Pillow.

### Запуск

```powershell
cd <project_root>/scripts
python salary_report.py
```

Дополнительный параметр: `python salary_report.py --month YYYY_MM`.

Перед запуском **закрыть Excel** с открытым `salary_*.xlsx` (иначе PermissionError).

## Структура кода (модули в файле)

| Функция | Что делает |
|---|---|
| `find_etalon()` | ищет эталон в `etalons/`, игнорирует `~$` lock-файлы |
| `read_xls(path)` | читает .xls через xlrd |
| `parse_akb()` | input/АКБ.xls → список dict |
| `parse_znr_op_total()` | все ЗНР за месяц |
| `parse_znr_op_stavka()` | ЗНР ОП со ставкой |
| `parse_znr_or_stavka()` | ЗНР ОР со ставкой |
| `parse_booking_exceptions()` | отказные/уточнения |
| `build_sheet_op_stavka()` | лист «ЗнР Ставка сыграла ОП» с группами |
| `build_sheet_or_stavka()` | лист «ЗнР Ставка сыграла ОР» |
| `build_sheet_znr_count()` | лист «ЗНР количество» |
| `build_sheet_akb_activity()` | лист «АКБ активность» с VLOOKUP |
| `build_sheet_booking()` | лист «Букинг_Отказ_Новый» (dedup) |
| `copy_constants()` | копирует константные листы (Условия МП/РОП, Справочник) |
| `insert_plan_jpg()` | встраивает скриншот плана в «План продаж» |
| `build_svod()` | заглушка (Свод не пересобирается) |
| `main()` | главный пайплайн |

## Справочники в коде (обезличенные плейсхолдеры)

```python
# Порядок подразделений ОП (6 шт., конкретные имена — на ваше усмотрение)
OP_PODRAZD_ORDER = [
    "ПОДРАЗДЕЛЕНИЕ_1",
    "ПОДРАЗДЕЛЕНИЕ_2",
    "ПОДРАЗДЕЛЕНИЕ_3",
    "ПОДРАЗДЕЛЕНИЕ_4",
    "ПОДРАЗДЕЛЕНИЕ_5",
    "ПОДРАЗДЕЛЕНИЕ_6",
]

# Посредники по подразделению (заполняется реальными именами при внедрении)
CARGO_BY_PODRAZD = {
    "ПОДРАЗДЕЛЕНИЕ_1": ["посредник_1", "посредник_2", "посредник_3"],
    "ПОДРАЗДЕЛЕНИЕ_2": ["посредник_4", "посредник_5"],
    "ПОДРАЗДЕЛЕНИЕ_3": ["посредник_6"],
    "ПОДРАЗДЕЛЕНИЕ_4": [],
    "ПОДРАЗДЕЛЕНИЕ_5": ["посредник_7", "посредник_8"],
    "ПОДРАЗДЕЛЕНИЕ_6": ["посредник_9", "посредник_10"],
}

# Шкала премии ОП (7 ступеней по % выполнения плана)
# Формат: (порог_выполнения, сумма_премии)
SCALE_OP = [
    (1.05, BONUS_TIER_1),  # >=105%
    (0.90, BONUS_TIER_2),  # >=90%
    (0.70, BONUS_TIER_3),  # >=70%
    (0.50, BONUS_TIER_4),  # >=50%
    (0.30, BONUS_TIER_5),  # >=30%
    (0.10, BONUS_TIER_6),  # >=10%
    (0.00, BONUS_TIER_7),  # <10%
]

# Шкала премии ОР (max по МДИ/клиентам)
SCALE_OR_BY_MDI = [...]  # заполняется по факту
```

## Листы эталона и кто их собирает

| Лист | Источник |
|---|---|
| Свод | копия эталона, формулы |
| ЗнР Ставка сыграла ОП | парсер |
| ЗнР Ставка сыграла ОР | парсер |
| План продаж | JPG встраивается |
| Букинг_Отказ_Новый | парсер с dedup |
| ФР - | копия эталона (ручные корректировки) |
| АКБ активность | парсер |
| ЗНР количество | парсер |
| Условия МП | копия эталона |
| Условия РОП | копия эталона |
| Справочник | копия эталона |

## Типичные задачи

### Запустить сборку
1. Закрой Excel, если открыт.
2. `python scripts/salary_report.py`.
3. Открой `output/salary_YYYY_MM.xlsx` в Excel.
4. Проверь лист «Свод» — формулы пересчитаются.

### Обновить эталон на новый месяц
1. Проверь `output/salary_YYYY_MM.xlsx` — если всё ОК.
2. Скопируй: `copy output\salary_YYYY_MM.xlsx etalons\YYYY_MM_salary.xlsx`.
3. Положи в `input/` файлы за новый месяц.
4. Запусти `python scripts/salary_report.py --month YYYY_MM`.

### Добавить нового менеджера в ОП
1. Добавь подразделение в `OP_PODRAZD_ORDER`.
2. Добавь строку в `CARGO_BY_PODRAZD` (если есть посредники).
3. Добавь ФИО в `read_op_managers_from_svod()` (читает из листа «Свод» эталона).

### Добавить посредника
1. Найди подразделение менеджера в `CARGO_BY_PODRAZD`.
2. Добавь имя контрагента в список.
3. Запусти сборку, проверь формулу COUNTIF на листе «ЗнР Ставка сыграла ОП».

### Диагностика: «премия не рассчитана»
Чаще всего причина — **SRC-маппинг сбит** (учётная система изменила формат выгрузки):
1. Открой `input/YYYY_MM_ЗНР_ОП_ставка.xls` — проверь шапку.
2. Открой `etalons/YYYY_MM_salary.xlsx` лист «ЗнР Ставка сыграла ОП» — сравни колонки.
3. Найди `SRC_OP = {...}` в `build_sheet_op_stavka()`, обнови.

### Диагностика: «формула возвращает 0 / #N/A»
1. Открой выходной файл в Excel, посмотри конкретную ячейку.
2. Проверь диапазон формулы (SUM, COUNTIF, VLOOKUP).
3. Если VLOOKUP — проверь, что значение `lookup_value` действительно есть в столбце `table_array`.

## Чего не делать

- ❌ Не редактируй эталон (`etalons/*.xlsx`) вручную без явной просьбы
- ❌ Не удаляй файлы из `input/` — это рабочие данные
- ❌ Не меняй формат имён файлов в `input/` без согласования (парсер ищет по подстрокам)
- ❌ Не выходи за пределы `<project_root>/`

## Что обезличено в этом шаблоне

- **Персональные данные:** имена руководителей подразделений, фамилии менеджеров → заменены на `ПОДРАЗДЕЛЕНИЕ_N`, `manager_N`.
- **Коммерческая информация:** названия контрагентов-посредников → `посредник_N`.
- **Схема мотивации:** конкретные суммы премий → `BONUS_TIER_N` (пороги в % оставлены как ориентир структуры).
- **Организационная структура:** РПУ/РОП → обезличены до ролей.

## Что адаптировать при внедрении

1. Заполнить реальные имена подразделений и менеджеров в `OP_PODRAZD_ORDER`.
2. Заполнить `CARGO_BY_PODRAZD` реальными контрагентами.
3. Подставить реальные суммы премий в `SCALE_OP` и `SCALE_OR_BY_MDI`.
4. Согласовать формат имён входных файлов с выгрузками учётной системы.
